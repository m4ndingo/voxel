# Wiki — pendiente de sincronizar

La wiki (`wiki/api.json`, `wiki/paginas/*.md`) **no se toca salvo que el dueño lo pida**.
Cuando un ticket cambie algo que la wiki documenta, se apunta **una línea aquí** y se sigue.
El dueño dirá cuándo hay que ponerse a actualizarla; entonces se vacía esta lista.

Formato: `- [ ] <ticket> · <qué cambió> → <dónde tocaría>`

- [ ] REQ-MOV1 · Móvil / Mandos · los mandos táctiles del Mundo ya traen **los tres botones del ratón** (⛏ romper = botón 0, ▣ poner = 2, ⚡ redstone = 1), el ✕ de salir pasó a ser un **menú ☰** (pantalla completa · foto · vídeo · salir) y 📷/🎬 dejaron de ser botones del pad para ser líneas de ese menú. Nuevo `game.pantallaCompleta()` (conmuta; va sobre `documentElement`, no sobre el canvas). El escritorio no cambia → tocaría la página de controles/atajos (donde se diga que en el móvil solo se puede andar y mirar, que ya no es cierto) y `api.json` (`game.pantallaCompleta`; `game.touchControls` sigue igual). Decir que desde el móvil romper/poner **sí** editan el mundo de verdad, con su autoguardado.
- [ ] REQ-GLOW5 · Partículas / Iluminación · los voxeles de `game.voxelesUI` pueden **alumbrar el terreno**, y el efecto se pide por **CLAVE DE MATERIAL**, no metiéndolo en el color: `game.voxelesUI.material(grupo, {emite:true, luz:6})` (mezcla, no reemplaza; `null` borra; clave desconocida avisa por consola), o por voxel con `pon(x,y,z,{col:…, emite:true})`. Siguen valiendo `'*#hex'` y `luz(grupo,n)`. Nuevo también `game.voxelesUI.luces` (interruptor de toda la luz de la capa) e `info()` con `luces`, `materiales` y `focosVivos` → tocaría `api.json` (`material`, `luz`, `luces`) y las páginas de partículas e iluminación, donde hoy se dice que la capa es «autoiluminada, sin luz» a secas: **autoiluminarse ≠ alumbrar**, y confundirlo es la pregunta que hizo el dueño. Avisar de que `'*' + [r,g,b]` no es un color emisivo (concatena el array y sale blanco).
- [ ] BUG-GLOW4 · Iluminación · un objeto emisivo **alumbra igual llevado en la mano** que plantado (antes no alumbraba nada en la mano). Sin mandos nuevos; `game.glowFocus` ahora **encarece** el camino fuera del haz en vez de anularlo, así que un foco alto ya no deja a oscuras lo que queda detrás → tocaría la página de iluminación (descripción de `game.glowFocus`) y la de herramienta-en-mano.
- [ ] REQ-SNP6 · Snippets · el panel Código tiene ahora **buscador** (busca el texto DENTRO del código de todos los snippets, no solo en el rótulo) y un botón **🔗 Usos** que dice qué snippets llaman al abierto —distinguiendo **llamada** (`game.snippet('x')`) de **mención** (el id en un comentario o una tabla)— y avisa cuando quien lo ejecuta es el motor (`mundo-<mapa>`, `arranque-<mapa>`…). API: `GET /api/snippets?q=<texto>` y `?usa=<id>`.

- [ ] BUG-SNP5 · Snippets · `game.mapName` (y todo lo que se deriva del nombre del mapa: `mundo-<mapa>`, `arranque-<mapa>`) devuelve ahora el **slug canónico**, el mismo que usa el servidor para el fichero del mundo: en `/map/fornite_c2_island` vale `fornite-c2-island`, no el tramo crudo de la URL. Y el **id** de un snippet ya se ve y se puede cambiar en el panel Código (cambiarlo = mover el fichero: quien llame `game.snippet('<id viejo>')` deja de encontrarlo) → tocaría `api.json` (`game.mapName`, `game.snippet`) y la página del panel Código.

- [ ] Pegado (Ctrl+V) · la fila de `Ctrl`+`V` en «Colocar, girar y deshacer» de `atajos.md` está entera desactualizada: `r` recorre las **6 caras** y `Shift`+`R` los **4 giros** (24 posturas, como las estructuras); **planta el clic DERECHO** y el izquierdo suelta; el cúmulo **sigue cargado tras plantar** (pegado continuo); y `Ctrl` + ratón elige el **agarre**, como en la herramienta volumen.

- [ ] Selección · los corchetes de esquina de la herramienta Seleccionar ya no son líneas: son **cubos de voxeles** (`game.selVoxeles` = `{activo, tam, largo}`, en voxeles finos; `false` vuelve a las líneas). Y hay una capa nueva para plantar **voxeles de color** a mano desde F12/snippets: `game.voxelesUI` (`pon`/`quita`/`limpia`/`linea`/`caja`/`info`, por grupos; no es mundo: no colisiona ni se guarda) → tocaría `api.json` y la página de la herramienta Seleccionar.

- [ ] Pegado (Ctrl+V) · con el pegado cargado, las teclas `1`…`9` ya **no** rellenan la selección de atrás: cambian el material **del cúmulo que se previsualiza** (y así queda para las copias siguientes). Además los corchetes de voxeles se dibujan sobre el pegado, no sobre la caja del Ctrl+C → tocaría `atajos.md` y la página de la herramienta Seleccionar.

- [ ] REQ-CART4 · Carteles/notas · el panel de la tecla `N` tiene una propiedad más, el **tinte**: seis muestras de post-it, `∅` (sin tinte) y un selector de color RGB a medida; el cartel entero se tiñe conservando la veta y el texto. Es **por nota** (no como `game.carteles`, que es global al mundo) y se guarda con ella (`noteTints`) → tocaría la página de notas/carteles y, si se documenta, la tabla de propiedades de la nota.

- [ ] BUG-TOOL3 · Herramienta en mano · mando nuevo **`game.toolAlphaFix`** (por defecto `true`): si el dibujo que se lleva en la mano tiene voxeles translúcidos (`#rrggbbaa`, p. ej. `hab:caja-de-volumen`), su parte de cristal se dibuja con una pasada muda de profundidad y otra de color con `LEQUAL`, así se ve **solo su cara más cercana** en vez del amontonamiento sin ordenar de la pasada de cristal del mapa. `false` = comportamiento viejo (sirve para comparar y para medir su coste) → tocaría `api.json` y `herramienta-en-mano.md`.

- [ ] REQ-TOOL6 · Herramienta en mano · la **rosca del ratón** recorre las herramientas en el Mundo (antes no hacía nada): hacia abajo baja por `pintar → seleccionar → volumen → pico → **mano vacía**` (o sea: la rosca es también la que **guarda** la herramienta), y hacia arriba al revés; en los extremos se para (no da la vuelta). Mandos `game.showTool.rueda` (on/off), `.ruedaUmbral` y `.escalera` → tocaría `atajos.md` (tabla del ratón), `herramienta-en-mano.md` y `api.json`.

- [ ] REQ-TOOL5 · Herramienta en mano · la herramienta se **guarda** (baja apuntando al suelo hasta salir de pantalla) y se vuelve a sacar; también la saca cambiar de herramienta. Guardada = **herramienta nula**: no rompe, no pone, no pinta, no marca, no dibuja guías ni cajas (se descarta lo que hubiera a medias, incluida la selección confirmada) y la ranura 10 se ve vacía. Mandos: `game.showTool.guardar` (`ms`, `baja`, `giro`, `aparta`, `curva`) + atajos planos, estado en `game.showTool.guardada`. ⚠️ El disparador **no** es el botón central (lo fue unas horas): es la **rosca** hacia abajo hasta «vacía» → tocaría `api.json`, `atajos.md` (tabla del ratón) y `herramienta-en-mano.md`.

- [ ] REQ-TOOL7 · Herramienta en mano · cambiar de herramienta **con la rosca** ya no es un salto: la vieja **baja** hasta salir de pantalla y la nueva **sube** detrás (mando `game.showTool.ruedaGesto`, `false` = cambio instantáneo). Y el **botón central** del ratón vuelve a ser **solo** el de redstone: conmuta palancas/botones/placas apuntando a ellos, calladito si no hay nada que conmutar (sigue sin aviso «ahí no hay nada que conmutar»), y ya **no** guarda ni saca la herramienta → tocaría `atajos.md` (tabla del ratón), `herramienta-en-mano.md`, `api.json` y la página de redstone.

- [ ] REQ-TOOL8 · Herramientas · **capa nueva entera**: `game.herramientas` (`define`/`quita`/`activa`/`golpea`/`lista`/`info`) permite crear desde un snippet una herramienta que no existe en el motor —dibujo en la mano, postura, alcance, gesto del clic, animación propia (`anima`) y acciones `izquierdo(c)`/`derecho(c)` en JavaScript—, ponerla en la mano con `game.playerTool` y verla como un peldaño más de la rosca. Es **pasiva**: no rompe ni pone nada por su cuenta. Snippet de demo: `herramienta-espada` → tocaría `api.json`, `herramienta-en-mano.md` y probablemente una página propia (es la primera vez que el catálogo de herramientas deja de ser cerrado).

- [ ] REQ-SPLIT1 · Gestor de snippets · **pantalla dividida**: el Mundo arriba y el editor de código abajo, para darle a ▶ y ver el cambio en directo. Botón «⬍ Dividir» en la cabecera del gestor, atajo **`Alt+D`** (con el editor cerrado lo abre ya dividido) y una **barra arrastrable** entre los dos para repartir la altura (doble clic = a la mitad); el reparto se recuerda. Dividido, la interfaz se compacta para dejar sitio al código: **desaparecen** el subtítulo de la cabecera y el pie con la chuleta de materiales (`setVoxel(x,y,z,material) · stone · smooth_stone · …`), y la lista de snippets se estrecha. Dividido el Mundo **se sigue jugando**: manda el foco (si escribes en el código, la `w` escribe; si haces clic en el juego, anda) → tocaría `atajos.md` (Alt+C ya está ahí, falta Alt+D) y la página del gestor de snippets, si la hay.

- [ ] BUG-AG19 · Agentes articulados / Herramientas · la **mira ya se para en un agente articulado** (antes el rayo lo atravesaba y la espada decía «tajo al aire»), y `game.esqueletos` tiene **dos métodos nuevos** para las herramientas de snippet: `enLaMira()` y `enPunto(x,y,z)`, que devuelven `{id, nombre, agente, agenteId, pieza, texto, punto, dist, local, dim, alto, rig, s, parte}` — `local` es el **voxel dentro del dibujo de la pieza** donde ha caído la acción (índices del editor) y `alto` esa altura en 0..1. Sin mandos nuevos y sin tocar `app.js` → tocaría `api.json` (la ficha de `game.esqueletos`) y la página de agentes; de paso, la de `game.herramientas` si se documenta el `c` del clic (ahí es donde se nota que una pieza de agente no trae `clave`).

- [x] ~~REQ-SANGRE1~~ · **hecho el 2026-08-19** con REQ-SNP-LIB1/2: el motor salió del snippet de la espada
  a librería propia, y la wiki tiene ya **sección propia «Partículas y efectos»** con la página
  `particulas.md` (los tres snippets, cómo hacerse uno, lluvia/nieve que te siguen, estrellas
  autoiluminadas, el coste medido y las sondas por forma) más la ficha de `game.voxelesUI` en `api.json`.
  Lo de abajo queda por si hace falta rastrear de dónde venía:
  <details><summary>nota original</summary>el snippet `herramienta-espada` trae ahora un **motor de partículas propio, `game.sangre`**: al acertar un tajo salta sangre **del punto exacto de la herida** (el que da `game.esqueletos.enLaMira().punto`) en forma de voxeles de **1×1×1** (`game.voxelesUI`, grupo `sangre`, `tam:1` = 1/16 de bloque), que caen, rebotan, se posan en el suelo y se quedan **30 s** antes de desvanecerse. Mandos: `activa`, `chorro`, `dura`, `desvanece`, `grav`, `fuerza`, `rebote`, `vuelo`, `tope`, más `salpica(punto)`/`limpia()`/`info()`. **No es motor**: no hay ni una línea nueva en `app.js`, y por eso es el ejemplo de manual de lo que `game.herramientas` + `game.voxelesUI` permiten hacer desde un snippet → tocaría `api.json` y la página de `game.herramientas`/herramienta-en-mano, como demo de la capa de decoración.</details>

- [ ] BUG-PASTE1 · Pegado (Ctrl+V) · la vista previa del cúmulo ya dibuja las **piezas finas** (repetidor, flores, antorchas, puertas…) con su **geometría de verdad**, no como un cubo macizo; y una pieza fina ya no tapa la cara del bloque de al lado en la previa. Sin mandos nuevos → tocaría, como mucho, la página de la herramienta Seleccionar (nota de que la previa es fiel a lo que se planta).

*(Sincronización completada el 2026-08-18: herramienta Seleccionar al grupo principal (`e`), relleno de la selección por ranura `1`…`9`, relleno por clic derecho, `r`/`Shift`+`R` re-aplicando la postura, y `e` añadida a las teclas reservadas — en `atajos.md` y `herramienta-en-mano.md`.)*

*(Sincronización del 2026-08-17: incorporados `game.sunShade`, `game.interiorDark`, `game.uvInset`/`game.texInset`, `game.glowLevel`, `game.glowFocus` en `wiki/api.json` e `iluminacion.md`. **Retiradas el 2026-08-18** de esa misma tanda `game.topShade`, `game.faceShades` y `game.sideShades`: no existen en `app.js` — el sombreado por cara es la tabla fija `MC_FACES`, sin mando en caliente.)*

- `wiki/paginas/particulas.md` · `sondas-mundo` publica una sonda nueva, `S.cajaVacia(x0,y0,z0,x1,y1,z1)` (la fase GRUESA: «¿en toda esta caja no hay nada?»). Es lo que abarata la lluvia y la nieve; conviene junto a `S.solido` (REQ-SNP-LIB3, 2026-08-19).
- `wiki/paginas/particulas.md` · `sondas-mundo` publica también `S.refrescaEstructuras()` (índice grueso de lo estampado, una vez por frame si se sondea en bucle). Va con `S.cajaVacia` de la línea de arriba (REQ-SNP-LIB3, 2026-08-19).
- `wiki/api.json` + `wiki/paginas/particulas.md` · **`game.volatiles.ponCapa(x,y,z, alto, material)`** (con `quitaCapa`, `hayCapa`, `tamCapa`): tercera capa volátil, una celda ACHATADA a `alto`/16 **con la textura del material** — lo que la capa fina no puede dar (sus cajas son de un color, y copiar una textura pediría 16×16 = 256 cajas por bloque). Va en la pasada del terreno ⇒ niebla y sombra del sol correctas. Es lo que pinta el manto de `particulas-voxel` desde 2026-08-19; con ella se retiraron `mantoDetalle`, `mantoLoncha` y `mantoTope`, y `mantoDe` pasó a ser EL MATERIAL. Además, la capa fina horneada (`ponFino`/`ponCajaFina`) ahora se dibuja **con niebla**, no a todo color contra el horizonte.
- `wiki/api.json` + `wiki/paginas/iluminacion.md` · **`game.luzOcluye`** (BUG-GLOW6, 2026-08-20): la luz **dinámica** (la que sigue a un agente, la que llevas en la mano, las estrellas de `game.voxelesUI`) ya respeta los sólidos, como la horneada. Dos mitades: en el shader, una cara no recibe la luz que le da **por detrás**; en la CPU, una luz que **no ve al ojo** no se sube. Por defecto encendido; ponerlo a `false` devuelve el comportamiento viejo (útil para medir fps). Detalle en `docs/luz-y-sombra.md`.
- `wiki/api.json` + `wiki/paginas/iluminacion.md` · **`game.shadowSuave`** y **`game.sunShadeNoche`** (REQ-SHADOW3, 2026-08-20): mandos de la **sombra proyectada del sol**. El primero es el radio del filtrado en téxeles (**0 = borde duro**, 1 = como siempre, tope 4); el segundo, cuánto aprieta la sombra con la exposición (`game.luz`) a 0, interpolado con ella (`null` = misma fuerza a todas horas). Conviene aprovechar para dejar escrito **qué proyecta sombra y qué no**: el mapa de sombra tiene **una sola fuente, el sol**, y las luces dinámicas (la espada de luz, las estrellas) **no** proyectan sombra. Detalle en `docs/luz-y-sombra.md`.
